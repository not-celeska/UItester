<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UItester" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

        <item name="android:animation">@android:anim/bounce_interpolator</item>
    </style>

    <style name="Theme.UItester" parent="Base.Theme.UItester" />
</resources>